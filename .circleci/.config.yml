version: 2.1

orbs: 
  heroku: circleci/heroku@7.60.2
  cypress: cypress-io/cypress@1

jobs:
  build:
    docker:
      - image: circleci/node:14.19.0
    working_directory: ~/repo
    steps:
      - checkout
      # install dependencies
      - run:
          name: Install Dependencies
          command: npm install
      - cypress/run

workflows:
  heroku_deploy:
    jobs:
      - build
      - heroku/deploy-via-git: # Use the pre-configured job, deploy-via-git
          requires:
            - build
          filters:
            branches:
              only: ["main", "testing/circleci"]